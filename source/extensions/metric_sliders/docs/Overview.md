# Overview

This extension provides a small window with metric sliders (0–5) and a Save button.

- Saving is keyed by the currently loaded stage URL.
- Data is stored as JSON in the user profile directory.

