# Camera Controls

Adds a simple camera controls panel with navigation speed presets and sliders.

## Behavior

- Sets minimum movement speed to `0.01` on startup.
- Uses navigation tool + fly operation for WASD-style movement.
- Disables viewport RMB context menu so RMB drag controls are available.
- Exposes sliders for speed/acceleration tuning.
