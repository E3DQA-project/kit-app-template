# Overview

This extension scans a folder for `.usdz` files and lets you traverse them:

- Next / Previous buttons
- Index selector (0-based)
- Unload current stage before loading the next to reduce VRAM usage

